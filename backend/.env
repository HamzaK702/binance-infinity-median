# The port the server will run on. Defaults to 3001 if not specified.
PORT=3001

# The environment the application is running in. Can be "development" or "production". Defaults to "development".
NODE_ENV=development

# The origin allowed for Cross-Origin Resource Sharing (CORS). This is the URL of your frontend application. Defaults to "http://localhost:3000".
CORS_ORIGIN=http://localhost:3000

# The maximum number of Binance pairs to track. Defaults to 10.
MAX_PAIRS=10

# The interval for WebSocket heartbeats in milliseconds. Defaults to 30000 (30 seconds).
WS_HEARTBEAT_INTERVAL=30000